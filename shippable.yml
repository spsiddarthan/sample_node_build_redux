# Language setting
#language: node_js

# Version number
node_js:
  - "0.12"
  - 0.10.25
 
branches:
 only:
   - "default/image"
   - "default2"
   
services:
   - mysql
   
  
build:
 # pre_ci:
 #   - docker build -t=shippabledocker/sample_node .

  ci:
    - ps -eax
    - ls -al
    - echo $key1 and $key2 and $key3
    - echo $M2 and $M1
    - echo $invalid_env and $splchars
    - echo $foo
    - echo $key4

  #post_ci:
   # - docker commit $SHIPPABLE_CONTAINER_NAME shippabledocker/shiphitchcock2_sample_node
    #- docker push shippabledocker/shiphitchcock2_sample_node

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
      branches:
        only:
          - "default/image"
